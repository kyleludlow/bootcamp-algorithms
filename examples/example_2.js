// 2. reverse a string



var reverseMe = function(string, arr){

  if (string.length === 0){
    return
  }

  arr = arr || [];

arr.push(string.slice(string.length -1, -1));

  string.charAt(string.length)
}
